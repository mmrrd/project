<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.predictions.livecricketscore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.predictions.livecricketscore.UpcomingDetailActiviy"></activity>
        <activity android:name="com.predictions.livecricketscore.FantacyDetailActivity" />
        <activity android:name="com.predictions.livecricketscore.LiveMatchActivity" />
        <activity android:name="com.predictions.livecricketscore.CompletedMatchActivity" />
        <activity android:name="com.predictions.livecricketscore.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>